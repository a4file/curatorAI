# 아담 (Adam)

갤러리에서 작가 대신 큐레이션을 수행하는 AI 시스템입니다. 모델명은 "아담"입니다. 관객이 QR 코드로 접근하여 블랙보드 스타일 인터페이스에서 AI 큐레이터 아담과 대화할 수 있으며, 작품 이미지와 작가 노트를 참고하여 큐레이션을 제공합니다.

## 주요 기능

- 🎨 작품 이미지 및 작가 노트 기반 큐레이션
- 💬 터미널/CMD 스타일 웹 인터페이스
- 📱 QR 코드를 통한 모바일 접근
- 🧠 Hugging Face Vision LLM 모델 통합
- 📚 대화 기록 아카이빙

## 설치 방법

### 1. 의존성 설치

```bash
pip install -r backend/requirements.txt
```

### 2. 환경 설정

필요한 경우 `.env` 파일을 생성하여 모델 설정을 변경할 수 있습니다.

## 실행 방법

### 백엔드 서버 시작

프로젝트 루트 디렉토리에서 실행:

```bash
# 방법 1: run.py 실행 (권장)
python run.py
```

또는 uvicorn을 직접 사용:

```bash
# 방법 2: uvicorn으로 실행
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

또는 backend 디렉토리에서:

```bash
# 방법 3: backend/main.py 직접 실행
cd backend
python main.py
```

### 접근

- 사용자 인터페이스: `http://localhost:8000/`
- 관리 페이지 (QR 코드): `http://localhost:8000/admin`
- API 문서: `http://localhost:8000/docs`

## 프로젝트 구조

```
curatorAI/
├── backend/
│   ├── main.py                 # FastAPI 앱 진입점
│   ├── api/
│   │   ├── conversation.py     # 대화 API 엔드포인트
│   │   └── qr.py               # QR 코드 생성 API
│   ├── services/
│   │   ├── curator_service.py  # AI 큐레이터 로직
│   │   ├── data_service.py     # 데이터 로드 관리
│   │   └── archiving_service.py # 아카이빙 기능
│   ├── data/
│   │   ├── artworks.json       # 작품 메타데이터
│   │   └── archives/           # 대화 아카이브
│   └── requirements.txt        # Python 의존성
├── frontend/
│   ├── index.html              # 메인 HTML
│   ├── admin.html              # 관리 페이지
│   ├── css/
│   │   └── terminal.css        # 터미널 스타일
│   └── js/
│       ├── terminal.js         # 터미널 UI 로직
│       └── api.js              # API 클라이언트
├── img/                        # 작품 이미지
└── text/                       # 작가 노트
```

## 사용 방법

1. 서버를 시작합니다.
2. `/admin` 페이지에서 QR 코드를 확인합니다.
3. QR 코드를 블랙보드나 전시장에 게시합니다.
4. 관객이 QR 코드를 스캔하여 큐레이터 AI와 대화합니다.

## Hugging Face 모델 설정

기본 모델은 `microsoft/kosmos-2-patch14-224`입니다. 

다른 Vision LLM 모델을 사용하려면 `backend/services/curator_service.py`에서 `model_name`을 변경할 수 있습니다:

- LLaVA: `"llava-hf/llava-1.5-7b-hf"`
- InstructBLIP: `"Salesforce/instructblip-vicuna-7b"`

## 배포

### Render 배포

1. GitHub 저장소에 코드를 푸시합니다.
2. Render 대시보드에서 "New Web Service"를 선택합니다.
3. GitHub 저장소를 연결합니다.
4. 다음 설정을 사용합니다:
   - **Build Command**: `pip install -r backend/requirements.txt`
   - **Start Command**: `uvicorn backend.main:app --host 0.0.0.0 --port $PORT`
   - **Environment**: Python 3
5. 환경 변수 설정:
   - `OPENAI_API_KEY`: OpenAI API 키
   - `OPENAI_MODEL`: `gpt-4o-mini` (선택사항)
6. 배포가 완료되면 Render가 제공하는 URL을 사용합니다.

### 프로덕션 환경 권장사항

1. CORS 설정을 특정 도메인으로 제한하세요.
2. 환경 변수를 사용하여 서버 URL을 설정하세요.
3. HTTPS를 사용하세요 (QR 코드 접근 시 필수).
4. 정적 파일(이미지, CSS, JS)은 CDN을 고려하세요.

## 라이선스

이 프로젝트는 MIT 라이선스를 따릅니다.

